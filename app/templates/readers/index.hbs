<div class="card-pf card-pf-view card-pf-view-xs">
  <div class="card-pf-header ">
    <h1 class="row">
      <span class="col-xs-6">Readers list</span>
      <span class="col-xs-6 text-right">
        {{#link-to "readers.new" activeClass="active" class='btn btn-success'}} {{fa-icon 'plus'}} new reader {{/link-to}}
      </span>
    </h1>
  </div>
</div>
{{!--
<div class="row toolbar-pf mx-0">
  <div class="col-sm-12"> --}} {{!--
    <form class="toolbar-pf-actions ">
      <div class="form-group toolbar-pf-filter">
        <label class="sr-only" for="filter">ID</label>
        <div class="input-group select-group">
          <div class="form-group ">
            <div class="select ">
              <select class="btn btn-default" name="filterSelect" onchange={{action 'selectedFilter' value="target.value" }}>
                <option value="id">ID</option>
                <option value="name">Name</option>
                <option value="address">Address</option>
              </select>
              {{fa-icon 'angle-down'}}</div>
            <!-- /btn-group -->
            <input type="text" class="form-control" id="filter" placeholder="Filter By ID...">
          </div>
        </div>
        <!-- /input-group -->
      </div>
      <div class="form-group">
        <div class="dropdown btn-group">
          <div class="select">
            <select class="btn btn-default" name="sortSelect" onchange={{action 'selectedSort' value="target.value" }}>
              <option value="id">ID</option>
              <option value="name">Name</option>
              <option value="address">Address</option>
            </select>
            {{fa-icon 'angle-down'}}
          </div>
          <button class="btn btn-link" type="button">
            {{fa-icon 'sort-alpha-down'}}
          </button>
        </div>
      </div>
    </form> --}}
    <div class="row toolbar-pf-results">
      <div class="col-sm-12">
        <h5>{{count}} Results</h5>
      </div>
    </div>
    {{!-- </div>
</div> --}}
<div id="pf-list-var6" class="list-group list-view-pf">
  {{#each model as |reader|}} {{#reader-item reader=reader}} {{/reader-item}} {{/each}}
  <form class="content-view-pf-pagination  clearfix" id="" onsubmit="return false;">
    <div class="form-group">
      <div class="select">
        <select class="btn btn-default" name="resultsSelected" onchange={{action 'selectedResultsPerPage' value="target.value" }}>
          <option value="5">5</option>
          <option value="10" selected>10</option>
          <option value="15">15</option>
          <option value="25">25</option>
          <option value="50">50</option>
          <option value="100">100</option>
        </select>
        {{fa-icon 'angle-down'}}
      </div>
      <span>per page</span>
    </div>
    <div class="form-group">
      <span>
        <span class="pagination-pf-items-current">{{add (mult (sub page 1) size) 1}} - {{if (eq page lastPage) count}}{{if (not-eq page lastPage) (mult page size)}}</span>
        of
        <span class="pagination-pf-items-total">{{count}}</span>
      </span>
      <ul class="pagination pagination-pf-back">
        <li class={{if (lte page 1) "disabled"}}>
          <a href="#" title="First Page" onclick={{action "goToFirstPage" }}>
            {{fa-icon 'angle-double-left'}}
          </a>
        </li>
        <li class={{if (lte page 1) "disabled"}}>
          <a href="#" title="Previous Page" onclick={{action "goToPrevPage" }}>
            {{fa-icon 'angle-left'}}
          </a>
        </li>
      </ul>
      <label for="-page" class="sr-only">Current Page</label>
      <input class="pagination-pf-page" type="text" value="{{page}}" id="page" onkeyup={{action "goToPage"}}/>
      <span>of
        <span class="pagination-pf-pages">{{lastPage}}</span>
      </span>
      <ul class="pagination pagination-pf-forward">
        <li class={{if (gte page lastPage) "disabled"}}>
          <a href="#" title="Next Page" onclick={{action "goToNextPage" }}>
            {{fa-icon 'angle-right'}}
          </a>
        </li>
        <li class={{if (gte page lastPage) "disabled"}}>
          <a href="#" title="Last Page" onclick={{action "goToLastPage" }}>
            {{fa-icon 'angle-double-right'}}
          </a>
        </li>
      </ul>
    </div>
  </form>
</div>